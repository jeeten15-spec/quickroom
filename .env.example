DATABASE_URL="file:./dev.db"

# Demo admin — replace before any production deploy.
# Production must use a real identity provider and hashed credentials.
ADMIN_EMAIL="julia.r@example.org"
ADMIN_PASSWORD="demo-admin-2026"
ADMIN_SESSION_SECRET="replace-with-32-plus-random-characters"

NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# demo | production  — production never sends unless credentials exist
NOTIFICATION_MODE="demo"
WHATSAPP_MODE="demo"
CALENDAR_PROVIDER="local"
CRM_PROVIDER="local"

# WhatsApp Business Platform (leave empty in demo)
WHATSAPP_TOKEN=""
WHATSAPP_PHONE_NUMBER_ID=""
WHATSAPP_VERIFY_TOKEN=""
WHATSAPP_APP_SECRET=""

# Google Calendar (leave empty in demo)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_REFRESH_TOKEN=""
GOOGLE_CALENDAR_ID=""

# Email (leave empty in demo)
SMTP_HOST=""
SMTP_PORT=""
SMTP_USER=""
SMTP_PASS=""
EMAIL_FROM=""

# CRM placeholder
CRM_API_KEY=""
