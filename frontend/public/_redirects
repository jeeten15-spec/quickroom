# Source of truth is generated at build time by scripts/prerender-seo.mjs.
# chat-shell.html (not room.html) avoids Cloudflare Pages pretty-URL 308 /room/:id → /room.
/room/:id /chat-shell.html 200
/dashboard/ /dashboard 301
